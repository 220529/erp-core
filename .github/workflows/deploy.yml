name: Deploy erp-core

# 触发条件: 推送到 master 分支或手动触发
on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 检出代码
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. 创建 .env.prod 文件
      - name: Create .env.prod
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          cat > .env.prod << EOF
          # ACR 配置（供 docker-compose 使用）
          ACR_REGISTRY=${{ secrets.ACR_REGISTRY_VPC }}
          ACR_NAMESPACE=${{ secrets.ACR_NAMESPACE }}
          IMAGE_TAG=$IMAGE_TAG

          # 应用配置
          NODE_ENV=production
          PORT=3009
          APP_NAME=ERP-Core

          # 数据库配置 (使用容器名称)
          DB_TYPE=mysql
          DB_HOST=erp-mysql
          DB_PORT=3306
          DB_USERNAME=erp_user
          DB_PASSWORD=${{ secrets.DB_USER_PASSWORD }}
          DB_DATABASE=erp_core
          DB_SYNCHRONIZE=true
          DB_LOGGING=false

          # Redis 配置 (使用容器名称)
          REDIS_HOST=erp-redis
          REDIS_PORT=6379
          REDIS_DB=0
          REDIS_PASSWORD=${{ secrets.REDIS_PASSWORD }}

          # JWT 配置
          JWT_SECRET=${{ secrets.JWT_SECRET }}
          JWT_EXPIRES_IN=7d

          # 文件上传
          UPLOAD_MAX_SIZE=10485760
          UPLOAD_DEST=./uploads

          # 日志配置
          LOG_LEVEL=warn
          LOG_DIR=./logs
          EOF

      # 3. 上传代码到服务器
      - name: Upload code to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          source: "."
          target: /app/erp-core-build/
          rm: true

      # 4. SSH 到服务器构建并部署
      - name: Build and deploy on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          command_timeout: 30m
          script: |
            set -e  # 遇到错误立即退出
            
            echo "========================================="
            echo "开始部署 erp-core"
            echo "========================================="
            
            cd /app/erp-core-build
            
            # 登录 ACR (使用内网地址)
            echo "🔐 登录 ACR..."
            docker login --username=${{ secrets.ACR_USERNAME }} \
                        --password=${{ secrets.ACR_PASSWORD }} \
                        ${{ secrets.ACR_REGISTRY_VPC }}
            
            # 设置镜像标签
            IMAGE_TAG=${{ github.sha }}
            FULL_IMAGE_NAME=${{ secrets.ACR_REGISTRY_VPC }}/${{ secrets.ACR_NAMESPACE }}/erp-core:$IMAGE_TAG
            echo "📦 镜像标签: $IMAGE_TAG"
            echo "📦 完整镜像名: $FULL_IMAGE_NAME"
            
            # 构建镜像
            echo "🔨 开始构建镜像..."
            docker build --progress=plain -t $FULL_IMAGE_NAME . 2>&1 | tee build.log
            
            if [ $? -eq 0 ]; then
                echo "✅ 镜像构建成功"
            else
                echo "❌ 镜像构建失败，查看日志文件 build.log"
                tail -n 100 build.log
                exit 1
            fi
            
            # 推送镜像到 ACR
            echo "📤 推送镜像到 ACR..."
            docker push $FULL_IMAGE_NAME
            
            # 同时推送 latest 标签
            echo "🏷️  标记并推送 latest 标签..."
            docker tag $FULL_IMAGE_NAME \
                       ${{ secrets.ACR_REGISTRY_VPC }}/${{ secrets.ACR_NAMESPACE }}/erp-core:latest
            docker push ${{ secrets.ACR_REGISTRY_VPC }}/${{ secrets.ACR_NAMESPACE }}/erp-core:latest
            
            # 复制配置文件到部署目录
            echo "📋 复制配置文件..."
            cp docker-compose.prod.yml /app/erp-core/
            cp .env.prod /app/erp-core/
            
            # 部署
            echo "🚀 开始部署..."
            cd /app/erp-core
            
            # 拉取最新镜像
            echo "📥 拉取最新镜像..."
            docker compose --env-file .env.prod -f docker-compose.prod.yml pull
            
            # 停止旧容器
            echo "⏹️  停止旧容器..."
            docker compose --env-file .env.prod -f docker-compose.prod.yml down || true
            
            # 启动新容器
            echo "▶️  启动新容器..."
            docker compose --env-file .env.prod -f docker-compose.prod.yml up -d
            
            # 等待容器启动
            echo "⏳ 等待容器启动..."
            sleep 10
            
            # 查看状态
            echo "📊 查看容器状态..."
            docker compose --env-file .env.prod -f docker-compose.prod.yml ps
            
            # 查看容器日志（最后20行）
            echo "📜 查看容器日志..."
            docker compose --env-file .env.prod -f docker-compose.prod.yml logs --tail=20
            
            # 清理构建目录的悬空镜像
            echo "🧹 清理悬空镜像..."
            docker image prune -f || true
            
            echo "========================================="
            echo "✅ erp-core 部署完成!"
            echo "========================================="
