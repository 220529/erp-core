# 数据表设计汇总

> ERP-Core 精简版数据库设计 - 简洁而闭环

---

## 📊 核心实体清单

| 序号 | 实体 | 表名 | 说明 | 职责 |
|------|------|------|------|------|
| 1 | User | `users` | 用户表 | 系统用户（销售、设计师、工长、财务） |
| 2 | Company | `companies` | 公司表 | 多公司/门店支持 |
| 3 | Department | `departments` | 部门表 | 组织架构 |
| 4 | Role | `roles` | 角色表 | 权限管理 |
| 5 | Menu | `menus` | 菜单表 | 功能菜单 |
| 6 | RoleMenu | `role_menus` | 角色菜单关联表 | 角色权限配置 |
| 7 | **Customer** | `customers` | **客户表** | 客户信息与状态管理 |
| 8 | **CustomerFollow** | `customer_follows` | **客户跟进记录表** | 跟进历史 |
| 9 | **Material** | `materials` | **物料库** | 主材、人工基础数据 |
| 10 | **Product** | `products` | **产品套餐表** | 标准化装修套餐 ✨ |
| 11 | **ProductMaterial** | `product_materials` | **产品物料关联表** | 套餐包含的物料清单 ✨ |
| 12 | **Order** | `orders` | **订单表（含施工）** | 订单 + 施工管理 |
| 13 | **OrderMaterial** | `order_materials` | **订单物料明细表** | 订单材料清单 |
| 14 | **Payment** | `payments` | **收款记录表** | 财务收款管理 |
| 15 | File | `files` | 文件表 | 附件管理 |
| 16 | DictType | `dict_types` | 字典类型表 | 数据字典分类 |
| 17 | DictData | `dict_data` | 字典数据表 | 字典明细数据 |
| 18 | Log | `logs` | 日志表 | 操作日志 |
| 19 | CodeFlow | `code_flows` | 代码流程表 | 动态业务代码 |

**核心业务表**（加粗）：Customer、Order、Product、Material、Payment

---

## 🏗️ 完整 ER 图

```
                        ┌───────────────┐
                        │     User      │
                        │   (用户表)     │
                        └───────┬───────┘
                                │
                   ┌────────────┼────────────┐
                   │            │            │
           ┌───────▼───────┐    │    ┌───────▼────────┐
           │   Customer     │    │    │   Material     │
           │   (客户表)      │    │    │   (物料库)     │
           └───────┬────────┘    │    └───────┬────────┘
                   │             │            │
       ┌───────────┼─────────────┤            │ 可选关联
       │           │             │            │
┌──────▼──────┐    │    ┌────────▼────────┐   │
│CustomerFollow│    │    │    Product      │   │
│(跟进记录表)  │    │    │   (产品套餐表)   │   │
└──────────────┘    │    └────────┬────────┘   │
                    │             │ 1:N        │
                    │     ┌───────▼────────┐   │
                    │  N:1│ProductMaterial │N:1│
                    │     │(产品物料关联表)├───┘
                    │     └────────────────┘
                    │
                    │ 1:N
            ┌───────▼────────┐
            │     Order      │
            │   (订单表)      │
            │  含施工管理     │
            └───────┬────────┘
                    │
         ┌──────────┼──────────┐
         │ 1:N      │ 1:N      │
  ┌──────▼──────┐   │   ┌──────▼──────┐
  │OrderMaterial│   │   │  Payment    │
  │(订单物料表) │   │   │ (收款表)    │
  └─────────────┘   │   └─────────────┘
                    │
                    ▼
              订单状态流转
         draft → signed → in_progress → completed
```

---

## 📋 核心业务表详细设计

### 1️⃣ Customer（客户表）

**职责**：管理客户信息与状态

```typescript
{
  id: number;                    // 主键
  name: string;                  // 客户姓名
  mobile: string;                // 手机号
  address: string;               // 地址
  area: string;                  // 所属区域
  status: CustomerStatus;        // 客户状态
  salesId: number;               // 销售ID
  designerId: number;            // 设计师ID
  remark: string;                // 备注
  createdAt: Date;
  updatedAt: Date;
}
```

**状态流转**：
```
lead（线索）→ measured（已量房）→ quoted（已报价）→ signed（已签约）→ completed（已完工）
```

---

### 2️⃣ Product（产品套餐表）✨ 新增

**职责**：标准化装修套餐模板

```typescript
{
  id: number;                    // 主键
  code: string;                  // 产品编码（唯一）
  name: string;                  // 产品名称
  category: string;              // 产品分类（套餐、单项）
  roomType: string;              // 适用房型（一居、两居、三居）
  style: string;                 // 装修风格（现代简约、北欧、中式）
  costPrice: number;             // 成本价
  salePrice: number;             // 售价
  description: string;           // 产品描述
  status: ProductStatus;         // 产品状态（active/inactive）
  sort: number;                  // 排序
  remark: string;                // 备注
  createdAt: Date;
  updatedAt: Date;
}
```

**编码规则**：`PRD` + `YYYYMMDD` + `序号（4位）`  
**示例**：`PRD202510310001`

---

### 3️⃣ ProductMaterial（产品物料关联表）✨ 新增

**职责**：定义套餐包含哪些物料

```typescript
{
  id: number;                    // 主键
  productId: number;             // 产品ID（外键）
  materialId: number;            // 物料ID（可为空，支持自定义）
  materialName: string;          // 物料名称
  category: string;              // 物料类别（main-主材, labor-人工）
  quantity: number;              // 数量
  unit: string;                  // 单位
  price: number;                 // 单价
  amount: number;                // 小计金额
  remark: string;                // 备注
  createdAt: Date;
  updatedAt: Date;
}
```

---

### 4️⃣ Order（订单表）

**职责**：商务 + 施工管理

```typescript
{
  id: number;                    // 主键
  orderNo: string;               // 订单编号（唯一）
  customerId: number;            // 客户ID（外键）
  
  // 商务相关
  totalAmount: number;           // 订单总金额
  paidAmount: number;            // 已收金额
  costAmount: number;            // 成本金额
  salesId: number;               // 销售ID
  designerId: number;            // 设计师ID
  signedAt: Date;                // 签约时间
  
  // 施工相关
  foremanId: number;             // 工长ID ← 施工负责人
  startedAt: Date;               // 开工时间 ← 施工开始
  completedAt: Date;             // 完工时间 ← 施工结束
  
  status: OrderStatus;           // 订单状态
  remark: string;                // 备注
  createdAt: Date;
  updatedAt: Date;
}
```

**状态流转**：
```
draft（草稿）→ signed（已签约）→ in_progress（施工中）→ completed（已完工）
                                                      ↓
                                                cancelled（已取消）
```

**编码规则**：`ORD` + `YYYYMMDD` + `序号（4位）`  
**示例**：`ORD202510310001`

---

### 5️⃣ OrderMaterial（订单物料明细表）

**职责**：订单的物料清单

```typescript
{
  id: number;                    // 主键
  orderId: number;               // 订单ID（外键）
  materialId: number;            // 物料ID（可为空）
  materialName: string;          // 物料名称
  category: string;              // 物料类别（main-主材, labor-人工, addition-增项）
  quantity: number;              // 数量
  unit: string;                  // 单位
  price: number;                 // 单价
  amount: number;                // 小计金额
  remark: string;                // 备注
  createdAt: Date;
  updatedAt: Date;
}
```

**金额计算规则**：
```javascript
orderMaterial.amount = quantity × price;
order.totalAmount = sum(orderMaterials.amount);
```

---

### 6️⃣ Payment（收款记录表）

**职责**：财务收款管理

```typescript
{
  id: number;                    // 主键
  paymentNo: string;             // 收款单号（唯一）
  orderId: number;               // 订单ID（外键）
  type: PaymentType;             // 收款类型
  amount: number;                // 收款金额
  method: string;                // 收款方式
  status: PaymentStatus;         // 收款状态
  paidAt: Date;                  // 实际收款时间
  createdBy: number;             // 创建人ID
  remark: string;                // 备注
  createdAt: Date;
  updatedAt: Date;
}
```

**收款类型**：
- `deposit` - 定金
- `contract` - 合同款
- `design_fee` - 设计费
- `addition` - 增项款

**状态流转**：
```
pending（待确认）→ confirmed（已确认）
                 ↓
            cancelled（已取消）
```

**编码规则**：`PAY` + `YYYYMMDD` + `序号（4位）`  
**示例**：`PAY202510310001`

---

## 🔄 完整业务流程

### 流程图

```
1️⃣ 客户获取
   销售录入客户信息 → Customer (status='lead')

2️⃣ 客户跟进
   上门量房 → CustomerFollow (type='measure')
            → Customer (status='measured')

3️⃣ 方案报价
   设计师选择产品套餐 → Product
   系统创建订单草稿 → Order (status='draft')
   复制套餐物料到订单 → ProductMaterial → OrderMaterial
   设计师可微调明细 → OrderMaterial (update)
   → Customer (status='quoted')

4️⃣ 合同签约
   客户签约 → Order (status='signed', signedAt=NOW())
   创建定金收款 → Payment (type='deposit', status='pending')
   → Customer (status='signed')

5️⃣ 财务确认
   财务确认收款 → Payment (status='confirmed', paidAt=NOW())
   更新订单已收金额 → Order (paidAmount += amount)

6️⃣ 施工管理
   分配工长并开工 → Order (foremanId=xxx, status='in_progress', startedAt=NOW())
   施工完成验收 → Order (status='completed', completedAt=NOW())
   → Customer (status='completed')

7️⃣ 业务闭环
   客户完工 → 数据归档 ✅
```

---

## 📊 数据流转示例

### 张三的装修订单数据

```sql
-- 1. 客户表
Customer {
  id: 1,
  name: '张三',
  mobile: '13800138000',
  status: 'completed',
  salesId: 1001,
  designerId: 2001
}

-- 2. 产品套餐（选择的套餐）
Product {
  id: 1,
  code: 'PRD202510310001',
  name: '现代简约三居室标准套餐',
  salePrice: 120000,
  costPrice: 80000
}

-- 3. 订单表
Order {
  id: 1,
  orderNo: 'ORD202510310001',
  customerId: 1,
  totalAmount: 125000,  // 微调后金额
  paidAmount: 125000,
  costAmount: 80000,
  status: 'completed',
  salesId: 1001,
  designerId: 2001,
  foremanId: 3001,
  signedAt: '2025-10-10',
  startedAt: '2025-10-12',
  completedAt: '2025-12-10'
}

-- 4. 订单明细（从套餐复制并微调）
OrderMaterial [
  { id: 1, orderId: 1, materialName: '客厅地砖', quantity: 100, price: 200, amount: 20000 },  // 升级
  { id: 2, orderId: 1, materialName: '卧室地板', quantity: 60, price: 200, amount: 12000 },
  { id: 3, orderId: 1, materialName: '水电改造', quantity: 1, price: 15000, amount: 15000 },
  ...
]

-- 5. 收款记录
Payment [
  { id: 1, orderId: 1, type: 'deposit', amount: 50000, status: 'confirmed', paidAt: '2025-10-10' },
  { id: 2, orderId: 1, type: 'contract', amount: 60000, status: 'confirmed', paidAt: '2025-10-15' },
  { id: 3, orderId: 1, type: 'addition', amount: 15000, status: 'confirmed', paidAt: '2025-11-20' }
]
```

---

## 🔑 关键设计原则

### 1. 简洁性

❌ **删除了**：Project（项目表）- 过度设计，订单已包含施工管理  
✅ **保留了**：核心业务表，职责清晰

### 2. 闭环性

✅ **新增了**：Product、ProductMaterial - 补充"选套餐"业务环节  
✅ **完整流程**：Customer → Product（选套餐）→ Order → Payment

### 3. 灵活性

- Material：物料基础库
- Product：标准套餐模板
- OrderMaterial：订单明细（可从套餐复制，可手动调整）

### 4. 扩展性

- 通过 CodeFlow 实现业务逻辑热更新
- 通过 DictType/DictData 实现数据字典扩展
- 支持多公司/多门店（Company、Department）

---

## ✅ 总结

### 数据表统计

| 分类 | 表数量 | 说明 |
|------|--------|------|
| **核心业务表** | 8 张 | Customer、Product、ProductMaterial、Order、OrderMaterial、Payment、Material、CustomerFollow |
| **基础管理表** | 7 张 | User、Company、Department、Role、Menu、RoleMenu、Log |
| **系统配置表** | 4 张 | CodeFlow、DictType、DictData、File |
| **合计** | **19 张** | 精简而完整 ✅ |

### 核心优势

✅ **简洁** - 去除冗余的 Project，订单承载施工职责  
✅ **闭环** - 新增 Product 套餐，补充"选套餐"环节  
✅ **标准** - 套餐模板化，快速报价  
✅ **灵活** - 套餐可微调，适应个性化需求  
✅ **透彻** - 麻雀虽小，五脏俱全！

---

**版本**: v2.0（含产品套餐）  
**更新日期**: 2025-10-31  
**维护人**: ERP 开发团队


